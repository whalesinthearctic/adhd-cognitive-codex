services:
  - type: web
      name: adhd-cognitive-codex
          runtime: node
              buildCommand: echo "No build needed"
                  startCommand: node server.js
                      envVars:
                            - key: ADMIN_TOKEN
                                    generateValue: true
                                          - key: NODE_ENV
                                                  value: production
